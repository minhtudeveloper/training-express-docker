import { User, UserDocument } from "../user/model";

const login = (body: any) => {
  return new Promise(async (rs, rj) => {
    try {
      
    } catch (error) {
      rj(error);
    }
  });
};

export default {
  login,
};
